# Final

Consolidated, reviewed dataset exported to training-ready JSONL via dataset/scripts/export_jsonl.py (CyberShield_Dataset_v1.jsonl). This folder is the source for the train/validation split used in LoRA fine-tuning.
